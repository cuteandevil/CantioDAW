{
  "name": "@cantiodaw/ts-orchestrator",
  "version": "0.1.0",
  "description": "CantioDAW AI Orchestrator — LLM toolchain via MCP, standalone exe",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "cantiodaw-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "build:prod": "tsc -p tsconfig.prod.json",
    "start": "node dist/index.js",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "node dist/index.js --test",
    "test:ollama": "node scripts/test-ollama.mjs",
    "bundle": "esbuild dist/index.js --bundle --platform=node --format=cjs --outfile=dist/bundle.cjs --external:none",
    "obfuscate": "javascript-obfuscator dist/bundle.js --output dist/obfuscated.js --compact true --control-flow-flattening true --control-flow-flattening-threshold 0.75 --numbers-to-expressions true --simplify false --string-array-encoding rc4 --string-array-threshold 0.8 --transform-object-keys true --identifier-names-generator mangled --self-defending true",
    "sea": "node --experimental-sea-config sea-config.json && node -e \"process.platform==='win32'?require('child_process').execSync('copy /Y \\\"'+process.execPath+'\\\" ..\\\\release\\\\cantiodaw-mcp.exe'):require('child_process').execSync('cp \\\"'+process.execPath+'\\\" ../release/cantiodaw-mcp')\" && npx postject ../release/cantiodaw-mcp.exe NODE_SEA_BLOB sea-prep.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2",
    "release": "node scripts/build-release.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.23.1",
    "javascript-obfuscator": "^4.2.2",
    "postject": "^1.0.0-alpha.6",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
